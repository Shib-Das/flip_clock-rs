[target.x86_64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y libxi-dev libgl1-mesa-dev libasound2-dev pkg-config"
]

[target.x86_64-pc-windows-gnu]
pre-build = [
    # Macroquad usually statically links or uses standard libs, but for cross we might need mingw setup?
    # Actually, macroquad on windows usually just needs GDI/User32 which are standard.
    # However, if we use some dependencies they might need more.
    # We'll leave it simple for now and see if default mingw toolchain in cross works.
]
